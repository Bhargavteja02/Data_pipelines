version: 2

models:
  - name: dim_movies
    columns:
      - name: movie_id
        tests:
          - not_null
          - unique

  - name: fct_ratings
    columns:
      - name: user_id
        tests:
          - not_null

      - name: movie_id
        tests:
          - not_null

      - name: rating
        tests:
          - not_null